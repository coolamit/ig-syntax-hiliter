
.phpunit.cache
vendor/
.env
.env.production
*.log

# OS
.DS_Store
Thumbs.db

# IDEs
.buildpath
.project
.settings/
.build/
.idea/
.vscode/

node_modules/

.scratchpad/*
.specs/*
.claude/
.ai/
.agents/
CLAUDE.md

# Compiled output: src/ -> build/, assets/src/ -> assets/build/. Nothing generated is committed.
# `build/` already matches assets/build/; the second line stays so it survives `build/` ever being anchored.
build/
assets/build/

# Tooling caches
.phpcs.cache
.phpunit.result.cache

# composer.lock and package-lock.json are committed on purpose. Do not ignore them.
